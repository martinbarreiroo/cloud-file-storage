name: Main Branch CI

on:
  pull_request:
    branches:
      - main
      - dev

jobs:
  test-and-lint:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'
          cache: 'npm'
      
      - name: Set up test database
        run: docker compose up db-test -d
      - name: Wait for DB to be ready
        run: sleep 10
          
      - name: Install dependencies
        run: npm ci
        
      - name: Run linting
        run: npm run lint
        
      - name: Check formatting
        run: npx prettier --check "src/**/*.ts" "test/**/*.ts"
        
      - name: Run tests
        run: npm test

      - name: Run e2e tests
        run: npm run test:e2e
        
      - name: Build project
        run: npm run build

  # Remove duplicate e2e-tests job if not needed, or fix it properly:
  # e2e-tests:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v3
  #     
  #     - name: Setup Node.js
  #       uses: actions/setup-node@v3
  #       with:
  #         node-version: '20'
  #         cache: 'npm'
  #     
  #     - name: Start test database
  #       run: docker compose up db-test -d
  #       
  #     - name: Wait for DB to be ready
  #       run: sleep 10
  #         
  #     - name: Install dependencies
  #       run: npm ci
  #       
  #     - name: Run e2e tests
  #       run: npm run test:e2e